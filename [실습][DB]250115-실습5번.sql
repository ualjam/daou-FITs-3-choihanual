select name, email from customers order by name, email desc;

select account_id, balance from accounts order by balance desc, account_id;

select amount, transaction_type from transactions order by transaction_type, amount desc;

select amount, status from loans order by status, amount desc;
