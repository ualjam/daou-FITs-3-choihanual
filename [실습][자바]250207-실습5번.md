Bonus
```
package javaexam2.company;

public interface Bonus {
    public default void incentive(int pay) {}
}

```

Company
```
package javaexam2.company;
import java.util.*;

public class Company {
    public static void main(String[] args) {
        HashMap map = new HashMap();

        Employee secretary = new Secretary("Hilery", 1, "secretary", 800);
        Employee sales = new Sales("Clinten", 2, "sales", 1200);

        map.put(secretary.getNumber(), secretary);
        map.put(sales.getNumber(), sales);

        for (Object key : map.keySet()) {
            Employee data = (Employee) map.get(key);

            System.out.println(data.getName() + " " + data.getDepartment() + " " + data.getSalary());
        }

        System.out.println("인센티브 100 지급");

//        ((Secretary) secretary).incentive(100);
//        ((Sales) sales).incentive(100);

        for (Object key : map.keySet()) {
            Employee data = (Employee) map.get(key);

            System.out.println(data.getName() + " " + data.getDepartment() + " " + data.getSalary() + " " + data.tax());
        }





    }
}

```

Employee
```
package javaexam2.company;

public abstract class Employee {

    private String name;
    private int number;
    private String department;
    private int salary;


    public Employee() {}

    public Employee(String name, int number, String department, int salary) {
        this.name = name;
        this.number = number;
        this.department = department;
        this.salary = salary;
    }

    public abstract double tax();

    public int getSalary() {
        return salary;
    }

    public void setSalary(int salary) {
        this.salary = salary;
    }

    public String getDepartment() {
        return department;
    }

    public void setDepartment(String department) {
        this.department = department;
    }

    public int getNumber() {
        return number;
    }

    public void setNumber(int number) {
        this.number = number;
    }

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }
}

```

Sales
```
package javaexam2.company;

public class Sales extends Employee implements Bonus{

    public Sales() {}
    public Sales(String name, int number, String department, int salary) {
        super(name, number, department, salary);
    }

    @Override
    public double tax() {
        return super.getSalary() * 0.13;
    }


    @Override
    public void incentive(int pay) {
        int salary = super.getSalary();

        super.setSalary(salary + (int) (pay * 1.2));
    }
}

```


Secretary
```
package javaexam2.company;

public class Secretary extends Employee implements Bonus{
    public Secretary() {}
    public Secretary(String name, int number, String department, int salary) {
        super(name, number, department, salary);
    }

    @Override
    public double tax() {
        return super.getSalary() * 0.1;
    }

    @Override
    public void incentive(int pay) {
        int salary = super.getSalary();

        super.setSalary(salary + (int) (pay * 0.8));
    }
}

```
